const styles = {
  container: {
    minHeight: 50,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 3,
  },
  thinContainer: {
    minHeight: 30,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 3,
  },
};

export default styles;
